# Ruta al JSON de la cuenta de servicio de Google (recomendado)
# export GOOGLE_APPLICATION_CREDENTIALS="/ruta/a/tu/service-account.json"
# Oponemos la variable para referencia (no subas el JSON al repo)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Modelo Gemini que quieres usar (ajusta según tu acceso)
GEMINI_MODEL=models/gemini-1.0

# Puerto opcional
PORT=3001
